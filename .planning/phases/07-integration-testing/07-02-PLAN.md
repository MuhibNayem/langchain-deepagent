---
phase: 07-integration-testing
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - luminamind/demos/__init__.py
  - luminamind/demos/frontend_demo.py
  - luminamind/demos/fullstack_demo.py
  - luminamind/demos/codereview_demo.py
  - luminamind/demos/runner.py
autonomous: true
requirements:
  - E2E-02

must_haves:
  truths:
    - "Three demo applications showcase harness capabilities"
    - "Frontend design demo shows UI generation quality"
    - "Full-stack app demo shows complete application generation"
    - "Code review demo shows bug detection capabilities"
    - "Demos are runnable via simple command"
  artifacts:
    - path: "luminamind/demos/__init__.py"
      provides: "Demo package exports"
      min_lines: 20
    - path: "luminamind/demos/frontend_demo.py"
      provides: "Frontend design demo using evaluator"
      exports: ["run_frontend_demo"]
    - path: "luminamind/demos/fullstack_demo.py"
      provides: "Full-stack application demo"
      exports: ["run_fullstack_demo"]
    - path: "luminamind/demos/codereview_demo.py"
      provides: "Code review demo using evaluator"
      exports: ["run_codereview_demo"]
    - path: "luminamind/demos/runner.py"
      provides: "Demo runner with nice output formatting"
      exports: ["DemoRunner", "run_all_demos"]
  key_links:
    - from: "luminamind/demos/runner.py"
      to: "luminamind/demos/frontend_demo.py"
      via: "import and call run_frontend_demo"
    - from: "luminamind/demos/runner.py"
      to: "luminamind/demos/fullstack_demo.py"
      via: "import and call run_fullstack_demo"
    - from: "luminamind/demos/runner.py"
      to: "luminamind/demos/codereview_demo.py"
      via: "import and call run_codereview_demo"
    - from: "luminamind/demos/*.py"
      to: "luminamind/deep_agent.py"
      via: "import DeepAgent"
---

<objective>
Create three demo applications that showcase the LuminaMind harness capabilities: frontend design demo, full-stack application demo, and code review demo. Each demo should be runnable and demonstrate the integrated Phase 1-6 components.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-integration-testing/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demos package structure</name>
  <files>luminamind/demos/__init__.py, luminamind/demos/runner.py</files>
  <read_first>
    - luminamind/__init__.py (package structure)
    - luminamind/deep_agent.py (DeepAgent interface)
  </read_first>
  <action>
    Create luminamind/demos/ directory and files:
    
    1. luminamind/demos/__init__.py:
       ```python
       """Demo applications for LuminaMind harness."""
       
       from luminamind.demos.runner import DemoRunner, run_all_demos
       
       __all__ = ["DemoRunner", "run_all_demos", "run_frontend_demo", "run_fullstack_demo", "run_codereview_demo"]
       ```
    
    2. luminamind/demos/runner.py with DemoRunner class:
       - Methods: run_frontend_demo(), run_fullstack_demo(), run_codereview_demo(), run_all_demos()
       - Each demo method prints header, runs demo, prints results
       - run_all_demos() runs all three in sequence
       - Nice console output with emoji and formatting
       - Error handling with user-friendly messages
  </action>
  <verify>
    <automated>python -c "from luminamind.demos import DemoRunner; print('Demo runner imports')"</automated>
  </verify>
  <done>Demo package structure created with runner</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend design demo</name>
  <files>luminamind/demos/frontend_demo.py</files>
  <read_first>
    - luminamind/deep_agent.py (DeepAgent interface)
    - luminamind/evaluator/evaluator.py (EvaluatorAgent interface)
  </read_first>
  <action>
    Create luminamind/demos/frontend_demo.py:
    
    run_frontend_demo() function that:
    
    1. Creates DeepAgent instance
    2. Runs a frontend design task: "Create a responsive landing page for a SaaS product with hero section, features grid, and pricing table"
    3. Uses the EvaluatorAgent to score the output
    4. Outputs:
       - Generated HTML/CSS code
       - Evaluation scores (design, craft, functionality)
       - Pass/fail determination
    
    The demo should:
    - Be self-contained (creates temp directory for output)
    - Clean up after itself
    - Show clear before/after of generation + evaluation
  </action>
  <verify>
    <automated>python -c "from luminamind.demos.frontend_demo import run_frontend_demo; print('Frontend demo imports')"</automated>
  </verify>
  <done>Frontend design demo creates and evaluates UI code</done>
</task>

<task type="auto">
  <name>Task 3: Create full-stack app demo</name>
  <files>luminamind/demos/fullstack_demo.py</files>
  <read_first>
    - luminamind/deep_agent.py (DeepAgent interface)
    - luminamind/planner/planner.py (PlannerAgent interface)
  </read_first>
  <action>
    Create luminamind/demos/fullstack_demo.py:
    
    run_fullstack_demo() function that:
    
    1. Creates DeepAgent instance
    2. Runs a full-stack app task: "Create a simple task management API with FastAPI backend and React frontend"
    3. Uses PlannerAgent to create a plan, then Generator to build
    4. Uses EvaluatorAgent to verify the implementation
    5. Outputs:
       - Generated project structure (tree view)
       - Key file contents
       - Evaluation scores
       - Whether app would run (syntax check)
    
    The demo should:
    - Generate real, working code
    - Include requirements.txt, main.py, components
    - Demonstrate the full Phase 1-6 pipeline
  </action>
  <verify>
    <automated>python -c "from luminamind.demos.fullstack_demo import run_fullstack_demo; print('Fullstack demo imports')"</automated>
  </verify>
  <done>Full-stack app demo generates and evaluates complete application</done>
</task>

<task type="auto">
  <name>Task 4: Create code review demo</name>
  <files>luminamind/demos/codereview_demo.py</files>
  <read_first>
    - luminamind/deep_agent.py (DeepAgent interface)
    - luminamind/evaluator/evaluator.py (EvaluatorAgent interface)
  </read_first>
  <action>
    Create luminamind/demos/codereview_demo.py:
    
    run_codereview_demo() function that:
    
    1. Creates DeepAgent instance
    2. Provides sample buggy code to evaluate:
       ```python
       # Sample buggy code with intentional issues
       def vulnerable_query(user_id):
           query = f"SELECT * FROM users WHERE id = {user_id}"  # SQL injection!
           return execute(query)
       
       def insecure_auth(password):
           return hash(password)  # No salt, deprecated hash!
       ```
    3. Uses EvaluatorAgent with code security criteria
    4. Outputs:
       - Detected issues with line numbers
       - Severity ratings
       - Suggested fixes
       - Overall security score
    
    The demo should demonstrate:
    - EvaluatorAgent detecting real bugs
    - Actionable feedback generation
    - Clear security scoring
  </action>
  <verify>
    <automated>python -c "from luminamind.demos.codereview_demo import run_codereview_demo; print('Codereview demo imports')"</automated>
  </verify>
  <done>Code review demo detects and reports security issues</done>
</task>

<task type="auto">
  <name>Task 5: Update CLI to support demos</name>
  <files>luminamind/main.py</files>
  <read_first>
    - luminamind/main.py (current CLI)
  </read_first>
  <action>
    Update luminamind/main.py to add demo commands:
    
    Add to argparse:
    - `luminamind demo` - Show available demos
    - `luminamind demo frontend` - Run frontend demo
    - `luminamind demo fullstack` - Run fullstack demo
    - `luminamind demo codereview` - Run code review demo
    - `luminamind demo all` - Run all demos
    
    Update main() to handle demo subcommands.
  </action>
  <verify>
    <automated>python -c "from luminamind.main import main; print('CLI updated with demo commands')"</automated>
  </verify>
  <done>CLI supports running demos with `luminamind demo [frontend|fullstack|codereview|all]`</done>
</task>

</tasks>

<verification>
1. `python -m luminamind demo` lists available demos
2. `python -m luminamind demo frontend` runs frontend demo
3. `python -m luminamind demo all` runs all demos sequentially
4. Each demo produces meaningful output showing harness capabilities
5. Demos clean up temporary files after execution
</verification>

<success_criteria>
- E2E-02: Demo applications (frontend design, full-stack app, code review) demonstrate quality
- `luminamind demo all` runs without errors
- Each demo produces evaluation scores and actionable feedback
- Demo output is clear and professionally formatted
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-testing/07-02-SUMMARY.md`
</output>
