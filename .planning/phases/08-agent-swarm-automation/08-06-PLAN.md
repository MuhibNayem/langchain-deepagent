---
phase: 08-agent-swarm-automation
plan: 08-06
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - luminamind/api/app.py
  - luminamind/api/routes/queue.py
  - luminamind/api/routes/scheduler.py
  - luminamind/api/routes/swarm.py
  - luminamind/api/auth.py
  - luminamind/api/middleware.py
  - luminamind/cli/swarm.py
  - tests/integration/test_api.py
autonomous: true
requirements:
  - SWARM-01
  - SWARM-07

must_haves:
  truths:
    - "REST API controls queue, scheduler, and swarm"
    - "All API endpoints require authentication"
    - "CLI provides equivalent control"
  artifacts:
    - path: "luminamind/api/app.py"
      provides: "FastAPI application with all routes"
      exports: "create_app"
    - path: "luminamind/cli/swarm.py"
      provides: "CLI commands for swarm control"
      exports: "swarm_cli"
  key_links:
    - from: "luminamind/api/routes/queue.py"
      to: "luminamind/queue/task_queue.py"
      via: "queue operations"
      pattern: "task_queue.enqueue|task_queue.dequeue"
---

<objective>
Build the REST API and CLI for external swarm control.
Purpose: External control surface for queue, scheduler, and swarm operations.
Output: FastAPI app with auth, rate limiting, and CLI group.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agent-swarm-automation/08-CONTEXT.md
@.planning/phases/08-agent-swarm-automation/08-RESEARCH.md
@.planning/phases/08-agent-swarm-automation/08-01-PLAN.md
@.planning/phases/08-agent-swarm-automation/08-02-PLAN.md
@.planning/phases/08-agent-swarm-automation/08-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FastAPI application with routes</name>
  <files>luminamind/api/app.py, luminamind/api/routes/queue.py, luminamind/api/routes/scheduler.py, luminamind/api/routes/swarm.py, luminamind/api/auth.py, luminamind/api/middleware.py, tests/integration/test_api.py</files>
  <action>
Create directory structure:
```
luminamind/api/
├── __init__.py
├── app.py
├── auth.py
├── middleware.py
└── routes/
    ├── __init__.py
    ├── queue.py
    ├── scheduler.py
    └── swarm.py
```

Create `luminamind/api/__init__.py`:
```python
from .app import create_app

__all__ = ["create_app"]
```

Create `luminamind/api/auth.py`:
```python
from fastapi import HTTPException, Security
from fastapi.security import APIKeyHeader
from typing import Optional

API_KEY_HEADER = APIKeyHeader(name="X-API-Key")

class APIKeyAuth:
    def __init__(self, api_keys: list[str]):
        self.api_keys = set(api_keys)
    
    async def __call__(self, api_key: str = Security(API_KEY_HEADER)):
        if api_key not in self.api_keys:
            raise HTTPException(status_code=401, detail="Invalid API key")
        return api_key

def create_api_key_auth() -> APIKeyAuth:
    # Load from environment
    import os
    keys = os.environ.get("LUMINAMIND_API_KEYS", "").split(",")
    return APIKeyAuth([k.strip() for k in keys if k.strip()])
```

Create `luminamind/api/middleware.py`:
```python
import time
from fastapi import Request
from starlette.middleware.base import BaseHTTPMiddleware

class RateLimitMiddleware(BaseHTTPMiddleware):
    def __init__(self, app, max_requests: int = 100, window_seconds: int = 60):
        super().__init__(app)
        self.max_requests = max_requests
        self.window_seconds = window_seconds
        self._requests: dict[str, list[float]] = {}
    
    async def dispatch(self, request: Request, call_next):
        client = request.client.host
        now = time.time()
        
        if client not in self._requests:
            self._requests[client] = []
        
        # Clean old requests
        self._requests[client] = [t for t in self._requests[client] if now - t < self.window_seconds]
        
        if len(self._requests[client]) >= self.max_requests:
            return Response(content="Rate limit exceeded", status_code=429)
        
        self._requests[client].append(now)
        return await call_next(request)
```

Create `luminamind/api/routes/queue.py`:
```python
from fastapi import APIRouter, Depends

router = APIRouter(prefix="/api/v1/queue", tags=["queue"])

@router.post("/enqueue")
def enqueue_task(task_type: str, payload: dict = {}, priority: str = "NORMAL"):
    # Implementation uses injected task_queue
    pass

@router.get("/status/{task_id}")
def get_task_status(task_id: str):
    pass

@router.delete("/cancel/{task_id}")
def cancel_task(task_id: str):
    pass

@router.get("/metrics")
def get_queue_metrics():
    pass
```

Create `luminamind/api/routes/scheduler.py`:
```python
from fastapi import APIRouter

router = APIRouter(prefix="/api/v1/scheduler", tags=["scheduler"])

@router.post("/schedule")
def schedule_task(task_type: str, cron: str = None, payload: dict = {}):
    pass

@router.get("/list")
def list_scheduled_tasks():
    pass

@router.delete("/unschedule/{task_id}")
def unschedule_task(task_id: str):
    pass

@router.put("/pause/{task_id}")
def pause_task(task_id: str):
    pass

@router.put("/resume/{task_id}")
def resume_task(task_id: str):
    pass
```

Create `luminamind/api/routes/swarm.py`:
```python
from fastapi import APIRouter

router = APIRouter(prefix="/api/v1/swarm", tags=["swarm"])

@router.post("/spawn")
def spawn_agent(role: str, config: dict = {}):
    pass

@router.delete("/kill/{agent_id}")
def kill_agent(agent_id: str):
    pass

@router.get("/status")
def get_swarm_status():
    pass

@router.post("/broadcast")
def broadcast_message(message_type: str, payload: dict = {}):
    pass

@router.get("/agents")
def list_agents():
    pass
```

Create `luminamind/api/app.py`:
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from .routes import queue, scheduler, swarm
from .auth import create_api_key_auth
from .middleware import RateLimitMiddleware

def create_app(task_queue=None, scheduler=None, swarm=None, auth=None) -> FastAPI:
    app = FastAPI(title="LuminaMind API", version="1.0.0")
    
    # CORS
    app.add_middleware(
        CORSMiddleware,
        allow_origins=["*"],
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )
    
    # Rate limiting
    app.add_middleware(RateLimitMiddleware, max_requests=100, window_seconds=60)
    
    # Auth
    auth_handler = auth or create_api_key_auth()
    
    # Routes
    app.include_router(queue.router)
    app.include_router(scheduler.router)
    app.include_router(swarm.router)
    
    @app.get("/health")
    def health():
        return {"status": "healthy", "version": "1.0.0"}
    
    return app
```

Add tests to `tests/integration/test_api.py`:
```python
import pytest
from fastapi.testclient import TestClient
from luminamind.api import create_app

def test_health_endpoint():
    app = create_app()
    client = TestClient(app)
    response = client.get("/health")
    assert response.status_code == 200
    assert response.json()["status"] == "healthy"

def test_auth_required():
    app = create_app()
    client = TestClient(app)
    response = client.get("/api/v1/queue/metrics")
    assert response.status_code == 401  # No API key

def test_queue_enqueue_with_auth():
    app = create_app()
    client = TestClient(app)
    # Would need valid API key
    response = client.post(
        "/api/v1/queue/enqueue?task_type=test",
        headers={"X-API-Key": "test-key"},
        json={"data": "test"},
    )
    assert response.status_code in (200, 500)  # 500 if no queue configured
```
  </action>
  <verify>
    <automated>pytest tests/integration/test_api.py -v --tb=short -k "health or auth"</automated>
  </verify>
  <done>
FastAPI app with queue/scheduler/swarm routes. Auth via API key header. Rate limiting middleware.
  </done>
</task>

<task type="auto">
  <name>Task 2: CLI group for swarm control</name>
  <files>luminamind/cli/swarm.py, luminamind/cli/__init__.py, tests/integration/test_api.py</files>
  <action>
Create `luminamind/cli/swarm.py`:
```python
import click
import requests
import os

API_BASE = os.environ.get("LUMINAMIND_API_URL", "http://localhost:8000")

@click.group(name="swarm")
def swarm_cli():
    """Swarm control commands."""
    pass

@swarm_cli.command("status")
def status():
    """Get swarm status."""
    api_key = os.environ.get("LUMINAMIND_API_KEY")
    if not api_key:
        click.echo("Error: LUMINAMIND_API_KEY not set")
        return
    
    response = requests.get(
        f"{API_BASE}/api/v1/swarm/status",
        headers={"X-API-Key": api_key},
    )
    if response.ok:
        data = response.json()
        click.echo(f"Active agents: {data['active_agents']}")
        click.echo(f"Idle agents: {data['idle_agents']}")
    else:
        click.echo(f"Error: {response.status_code}")

@swarm_cli.command("spawn")
@click.option("--role", required=True, help="Agent role")
@click.option("--config", default="{}", help="JSON config")
def spawn(role: str, config: str):
    """Spawn an agent."""
    import json
    api_key = os.environ.get("LUMINAMIND_API_KEY")
    if not api_key:
        click.echo("Error: LUMINAMIND_API_KEY not set")
        return
    
    response = requests.post(
        f"{API_BASE}/api/v1/swarm/spawn?role={role}",
        headers={"X-API-Key": api_key},
        json=json.loads(config) if config != "{}" else {},
    )
    if response.ok:
        click.echo(f"Agent spawned: {response.json()['agent_id']}")
    else:
        click.echo(f"Error: {response.status_code}")

@swarm_cli.command("kill")
@click.argument("agent_id")
def kill(agent_id: str):
    """Kill an agent."""
    api_key = os.environ.get("LUMINAMIND_API_KEY")
    if not api_key:
        click.echo("Error: LUMINAMIND_API_KEY not set")
        return
    
    response = requests.delete(
        f"{API_BASE}/api/v1/swarm/kill/{agent_id}",
        headers={"X-API-Key": api_key},
    )
    if response.ok:
        click.echo(f"Agent {agent_id} killed")
    else:
        click.echo(f"Error: {response.status_code}")

@click.group(name="queue")
def queue_cli():
    """Queue control commands."""
    pass

@queue_cli.command("enqueue")
@click.option("--type", required=True, help="Task type")
@click.option("--payload", default="{}", help="JSON payload")
@click.option("--priority", default="NORMAL", help="Priority")
def enqueue(type: str, payload: str, priority: str):
    """Enqueue a task."""
    import json
    api_key = os.environ.get("LUMINAMIND_API_KEY")
    if not api_key:
        click.echo("Error: LUMINAMIND_API_KEY not set")
        return
    
    response = requests.post(
        f"{API_BASE}/api/v1/queue/enqueue?task_type={type}&priority={priority}",
        headers={"X-API-Key": api_key},
        json=json.loads(payload),
    )
    if response.ok:
        click.echo(f"Task enqueued: {response.json()['task_id']}")
    else:
        click.echo(f"Error: {response.status_code}")

@queue_cli.command("status")
@click.argument("task_id")
def task_status(task_id: str):
    """Get task status."""
    api_key = os.environ.get("LUMINAMIND_API_KEY")
    if not api_key:
        click.echo("Error: LUMINAMIND_API_KEY not set")
        return
    
    response = requests.get(
        f"{API_BASE}/api/v1/queue/status/{task_id}",
        headers={"X-API-Key": api_key},
    )
    if response.ok:
        click.echo(f"Task {task_id}: {response.json()['status']}")
    else:
        click.echo(f"Error: {response.status_code}")

@click.group(name="luminamind")
def main():
    """LuminaMind CLI."""
    pass

main.add_command(swarm_cli)
main.add_command(queue_cli)

if __name__ == "__main__":
    main()
```

Create `luminamind/cli/__init__.py`:
```python
from .swarm import main, swarm_cli, queue_cli

__all__ = ["main", "swarm_cli", "queue_cli"]
```
  </action>
  <verify>
    <automated>python -c "from luminamind.cli import main; print('CLI imports OK')"</automated>
  </verify>
  <done>
CLI groups for swarm and queue operations. API key from environment variable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external→API | Untrusted client requests cross here — auth required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | S | API endpoints | mitigate | All endpoints require valid API key |
| T-08-06 | S | Rate limiting | mitigate | Max 100 req/min per client IP |
| T-08-06 | I | CLI credentials | accept | CLI reads from env — user responsible for env security |
</threat_model>

<verification>
1. `pytest tests/integration/test_api.py -v --tb=short`
2. Manual: Start API, test endpoints with curl
</verification>

<success_criteria>
- /api/v1/queue/enqueue creates task (requires auth)
- /api/v1/swarm/spawn creates agent (requires auth)
- Rate limiting returns 429 when exceeded
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-swarm-automation/08-06-SUMMARY.md`
</output>
