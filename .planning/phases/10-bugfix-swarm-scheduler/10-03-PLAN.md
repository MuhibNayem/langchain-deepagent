---
phase: 10
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - luminamind/planner/bounded_subagent.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "BoundedSubagent imports create_deep_agent from luminamind.deep_agent"
    - "create_deep_agent is called correctly with agent_kwargs"
  artifacts:
    - path: "luminamind/planner/bounded_subagent.py"
      provides: "Fixed import of create_deep_agent"
      contains: "from luminamind.deep_agent import create_deep_agent"
  key_links:
    - from: "BoundedSubagent.__init__()"
      to: "luminamind.deep_agent"
      via: "create_deep_agent import"
      pattern: "create_deep_agent = create_deep_agent"
---

<objective>
Fix the BoundedSubagent import bug. Line 136 calls `create_deep_agent()` but it's using the wrong reference. The `create_deep_agent` function is defined in `luminamind.deep_agent` module but not properly imported in bounded_subagent.py.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix create_deep_agent import in BoundedSubagent</name>
  <files>luminamind/planner/bounded_subagent.py</files>
  <read_first>luminamind/planner/bounded_subagent.py</read_first>
  <action>
In bounded_subagent.py, the problematic code is at line 136:

```python
self.agent = create_deep_agent(**agent_kwargs)
```

But `create_deep_agent` is NOT imported - the module has no import statement for it. The function is defined in `luminamind/deep_agent.py` as:

```python
def create_deep_agent(config: DeepAgentConfig | None = None) -> DeepAgent:
```

Add the import at the top of bounded_subagent.py, after line 120 (after the `from luminamind.llm import get_llm` import):

```python
from luminamind.deep_agent import create_deep_agent
```

Then verify line 136 is unchanged (`self.agent = create_deep_agent(**agent_kwargs)`) - it should work once the import is added.
  </action>
  <verify>
grep -n "from luminamind.deep_agent import create_deep_agent" luminamind/planner/bounded_subagent.py
python -c "
from luminamind.planner.bounded_subagent import BoundedSubagent
print('PASS: BoundedSubagent imports correctly')
" 2>&1 | head -5</verify>
  <done>BoundedSubagent imports create_deep_agent from luminamind.deep_agent</done>
  <acceptance_criteria>
- Import line: `from luminamind.deep_agent import create_deep_agent`
- Line 136: `self.agent = create_deep_agent(**agent_kwargs)` works without NameError
- BoundedSubagent can be instantiated without import errors
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Read bounded_subagent.py and verify the import fix
2. Run Python import test
3. Verify create_deep_agent is now defined in the module scope
</verification>

<success_criteria>
- bounded_subagent.py has: `from luminamind.deep_agent import create_deep_agent`
- No NameError on `create_deep_agent`
- bounded_subagent.py passes: `python -c "from luminamind.planner.bounded_subagent import BoundedSubagent; print('import ok')"`
</success_criteria>

<output>
After completion, create `.planning/phases/10-bugfix-swarm-scheduler/10-03-SUMMARY.md`
</output>