---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - luminamind/swarm/swarm.py
  - luminamind/multi_agent/message_bus.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Swarm uses agent_message_bus for inter-agent communication"
    - "Swarm._message_queue is synced with agent_message_bus"
  artifacts:
    - path: "luminamind/swarm/swarm.py"
      provides: "Swarm integration with agent_message_bus"
      contains: "from luminamind.multi_agent.message_bus import AgentMessageBus"
  key_links:
    - from: "Swarm.broadcast()"
      to: "AgentMessageBus"
      via: "message publishing"
      pattern: "self._message_bus"
---

<objective>
Integrate Swarm with the agent_message_bus system. Currently Swarm uses its own internal _message_queue list for message passing, but it should use the shared AgentMessageBus for proper inter-agent communication. This allows Swarm agents to communicate with other non-Swarm agents in the system.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AgentMessageBus integration to Swarm</name>
  <files>luminamind/swarm/swarm.py</files>
  <read_first>luminamind/swarm/swarm.py</read_first>
  <action>
Read the current Swarm class and understand the _message_queue pattern. 

The goal is to integrate with AgentMessageBus. Check if luminamind/multi_agent/message_bus.py exists:

```bash
ls -la luminamind/multi_agent/ 2>/dev/null || echo "multi_agent dir not found"
```

If AgentMessageBus exists:
1. Add import: `from luminamind.multi_agent.message_bus import AgentMessageBus`
2. In __init__, accept optional message_bus parameter
3. Store: `self._message_bus = message_bus or AgentMessageBus()`
4. Modify broadcast() and send_to() to use `self._message_bus.publish(message)` instead of appending to `self._message_queue`

If AgentMessageBus doesn't exist yet:
1. The Swarm class should accept a `message_bus` parameter with a sensible default (creating an internal one if None)
2. Change `self._message_queue: list[SwarmMessage] = []` to use message_bus internally
3. Keep backward compatibility - if no message_bus provided, create an internal one

For this fix, we'll make Swarm accept an optional message_bus parameter and use it for publishing. If not provided, create an internal AgentMessageBus-style interface.

Actually - read the existing Swarm code first. The current implementation uses an internal _message_queue list. The fix should:
1. Keep backward compatibility (Swarm can still work standalone)
2. Allow optional injection of external message_bus for integration
3. broadcast() and send_to() should use the message_bus if available

Let me structure this as:
- Add `message_bus` parameter to __init__ (default None)
- If message_bus is None, create a simple internal queue (current behavior)
- If message_bus is provided, use it for message dispatch
</action>
  <verify>
grep -n "message_bus" luminamind/swarm/swarm.py
python -c "
from luminamind.swarm.swarm import Swarm
s = Swarm()
print('PASS: Swarm instantiates without message_bus')
s2 = Swarm(message_bus=None)
print('PASS: Swarm accepts message_bus param')
"</verify>
  <done>Swarm accepts optional message_bus parameter for integration with AgentMessageBus</done>
  <acceptance_criteria>
- `__init__` accepts `message_bus` parameter
- Swarm works standalone when message_bus=None
- broadcast() and send_to() use message_bus if provided
- No breaking changes to existing behavior
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Read swarm.py and verify the message_bus integration
2. Run Python instantiation tests
3. Verify backward compatibility - Swarm without message_bus still works
</verification>

<success_criteria>
- Swarm accepts message_bus parameter in __init__
- Swarm can be instantiated with or without message_bus
- No breaking changes to existing Swarm usage
- swarm.py passes: `python -c "from luminamind.swarm.swarm import Swarm; print('import ok')"`
</success_criteria>

<output>
After completion, create `.planning/phases/10-bugfix-swarm-scheduler/10-04-SUMMARY.md`
</output>