---
phase: 11
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements: []

must_haves:
  truths:
    - "pytest can discover all tests without missing dependency errors"
  artifacts:
    - path: "pyproject.toml"
      provides: "package dependencies for test discovery"
      contains: "pytest"
  key_links:
    - from: "pyproject.toml"
      to: "tests/"
      via: "pytest_plugins or test"
      pattern: "testpaths|pytest"
---

<objective>
Fix package test collection issues. pytest is missing a required plugin or dependency for test discovery to work properly in this package. Add any missing test dependencies to pyproject.toml.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Run `python -m pytest --collect-only` to see which tests can be discovered and which fail.

Common issues:
- pytest plugins needed (pytest-asyncio, pytest-mock, etc.)
- testpaths not configured
- conftest.py not being loaded
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix test collection</name>
  <files>pyproject.toml</files>
  <action>
First, run `python -m pytest --collect-only 2>&1 | head -100` to diagnose the specific test collection error. Then fix pyproject.toml by adding any missing test dependencies.

Common fix: add pytest-asyncio if async tests exist, and ensure testpaths is set:
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
```

If specific imports fail (e.g., aioresponses, pytest-mock), add them to the dependencies.

After editing pyproject.toml, verify with:
```bash
python -m pytest --collect-only 2>&1 | grep -E "(error|ERROR|collected)" | head -20
```
</action>
  <verify>
<automated>python -m pytest --collect-only 2>&1 | tail -5</automated>
  </verify>
  <done>pytest collects tests without import errors</done>
</task>

</tasks>

<success_criteria>
pytest test collection works without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/11-bugfix-round-2/11-03-SUMMARY.md`
</output>