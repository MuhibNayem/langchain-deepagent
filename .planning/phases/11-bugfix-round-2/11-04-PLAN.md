---
phase: 11
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - luminamind/learning/skill_library.py
  - luminamind/streaming/token_stream.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "All modules import aiofiles consistently"
    - "Runtime does not fail on missing aiofiles"
  artifacts:
    - path: "pyproject.toml"
      provides: "runtime dependencies"
      contains: "aiofiles"
  key_links:
    - from: "luminamind/learning/skill_library.py"
      to: "aiofiles"
      via: "import aiofiles"
      pattern: "aiofiles"
    - from: "luminamind/streaming/token_stream.py"
      to: "aiofiles"
      via: "import aiofiles"
      pattern: "aiofiles"
---

<objective>
Fix runtime dependency consistency for aiofiles and other runtime libraries that may be imported in code but missing from pyproject.toml dependencies.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Find all imports of aiofiles and other runtime libs, then ensure they're in pyproject.toml.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find all aiofiles imports and add to dependencies</name>
  <files>pyproject.toml, luminamind/learning/skill_library.py, luminamind/streaming/token_stream.py</files>
  <action>
Search for all aiofiles imports in the codebase:
```bash
grep -r "import aiofiles\|from aiofiles" --include="*.py" .
```

Also check for other runtime deps that may be missing:
```bash
grep -r "from (aiofiles|httpx|aiohttp|aiosignal)" --include="*.py" . | head -20
```

Add missing runtime dependencies to pyproject.toml dependencies section. Example:
```toml
dependencies = [
    ...
    "aiofiles>=23.0.0",
]
```

If no imports of aiofiles found, verify that skill_library.py and token_stream.py don't actually need it — they may have been refactored.
</action>
  <verify>
<automated>python -c "import luminamind; print('Import successful')" 2>&1</automated>
  </verify>
  <done>All runtime dependencies present in pyproject.toml</done>
</task>

</tasks>

<success_criteria>
No ImportError for runtime dependencies at module load time.
</success_criteria>

<output>
After completion, create `.planning/phases/11-bugfix-round-2/11-04-SUMMARY.md`
</output>