---
phase: 11
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - luminamind/evaluator/db_verifier.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "DB verifier uses parameterized queries"
    - "No SQL injection is possible through any input"
  artifacts:
    - path: "luminamind/evaluator/db_verifier.py"
      provides: "SQL injection prevention"
      uses_parameterized_queries: true
  key_links:
    - from: "luminamind/evaluator/db_verifier.py"
      to: "database"
      via: "cursor.execute with parameterized queries"
      pattern: "cursor\\.execute.*%"
---

<objective>
Fix SQL injection vulnerability in DB verifier. The verifier may be building SQL queries via string formatting/concatenation instead of using parameterized queries. This is a critical security issue per SEC-01.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Read luminamind/evaluator/db_verifier.py and search for any raw SQL string building with %, format(), or f-strings that include user input.
</context>

<interfaces>
From luminamind/evaluator/db_verifier.py (need to read for SQL patterns):
```python
# Look for patterns like:
cursor.execute(f"SELECT * FROM {table}")  # DANGEROUS
cursor.execute("SELECT * FROM %s" % table)  # DANGEROUS
# vs safe:
cursor.execute("SELECT * FROM table_name WHERE id = %s", (id,))  # SAFE
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix SQL injection in DB verifier</name>
  <files>luminamind/evaluator/db_verifier.py</files>
  <action>
Read luminamind/evaluator/db_verifier.py and find all cursor.execute() calls.

Replace any raw SQL strings with user input using parameterized queries:

DANGEROUS patterns to fix:
- f"SELECT ... WHERE id = {user_input}"
- "SELECT ... WHERE id = %s" % user_input
- cursor.execute(sql_string + user_input)

SAFE pattern:
- cursor.execute("SELECT ... WHERE id = %s", (user_input,))

Apply parameterized queries to ALL cursor.execute() calls that include any dynamic input.

Also check for cursor.execute with % formatting:
- cursor.execute(sql % (arg1, arg2)) → cursor.execute(sql, (arg1, arg2))
</action>
  <verify>
<automated>python -c "from luminamind.evaluator.db_verifier import DbVerifier; print('Import successful')"</automated>
  </verify>
  <done>All SQL queries use parameterized queries, no string interpolation in SQL</done>
</task>

</tasks>

<success_criteria>
DB verifier passes SQL injection audit - all queries parameterized.
</success_criteria>

<output>
After completion, create `.planning/phases/11-bugfix-round-2/11-06-SUMMARY.md`
</output>