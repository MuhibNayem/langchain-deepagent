---
phase: 11
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - luminamind/queue/task_queue.py
  - luminamind/queue/memory_backend.py
  - luminamind/queue/redis_backend.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Delayed tasks are scheduled for future execution"
    - "delay parameter is respected (not ignored)"
  artifacts:
    - path: "luminamind/queue/task_queue.py"
      provides: "TaskQueue.enqueue with delay support"
      respects_delay: true
  key_links:
    - from: "luminamind/queue/task_queue.py"
      to: "luminamind/queue/memory_backend.py"
      via: "scheduled_at calculation"
      pattern: "scheduled_at|delay"
---

<objective>
Fix the memory queue delay bug. The delay parameter in enqueue is not being respected — tasks are being scheduled with utcnow() instead of now() + delay, causing delayed tasks to run immediately.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

From Phase 10 summary: "Delayed queue tasks scheduled_at bug (uses utcnow() instead of now + delay)"

Check the enqueue method in task_queue.py and the backend files to see where scheduled_at is calculated.
</context>

<interfaces>
From luminamind/queue/task_queue.py:
```python
def enqueue(self, task: Task, delay_seconds: int = 0) -> str:
    if delay_seconds > 0:
        task.scheduled_at = datetime.utcnow()  # BUG: should be datetime.now() + timedelta(seconds=delay)
```

Also check memory_backend.py and redis_backend.py for scheduled_at handling.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix delay calculation in TaskQueue.enqueue</name>
  <files>luminamind/queue/task_queue.py, luminamind/queue/memory_backend.py, luminamind/queue/redis_backend.py</files>
  <action>
Find where delay_seconds is used when calculating task.scheduled_at.

In task_queue.py enqueue method:
- Current (buggy): task.scheduled_at = datetime.utcnow()
- Should be: task.scheduled_at = datetime.now() + timedelta(seconds=delay_seconds)

Also check the backend dequeue methods — they should only return tasks where:
- task.scheduled_at is None (immediate) OR
- task.scheduled_at <= datetime.now()

Ensure datetime and timedelta are imported at the top of task_queue.py.
</action>
  <verify>
<automated>python -c "from luminamind.queue.task_queue import TaskQueue, Task; q = TaskQueue(); t = Task(task_id='test', name='test', func=lambda: None); q.enqueue(t, delay_seconds=5); print(f'Scheduled at: {t.scheduled_at}')"</automated>
  </verify>
  <done>Delayed tasks are scheduled for now() + delay_seconds, not utcnow()</done>
</task>

</tasks>

<success_criteria>
Enqueue with delay_seconds=5 results in scheduled_at being ~5 seconds in the future.
</success_criteria>

<output>
After completion, create `.planning/phases/11-bugfix-round-2/11-07-SUMMARY.md`
</output>